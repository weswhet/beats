input {
  beats {
    host => "0.0.0.0"
    port => 5044
  }
}

output {
  file {
    path => "${FILEBEAT_UNIFIEDLOGS_E2E_OUTPUT:/tmp/filebeat-unifiedlogs-e2e-events.jsonl}"
    codec => json_lines
  }
}
